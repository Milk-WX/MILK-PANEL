# Milk Platform v2.0

## 发布范围

这是当前 VPS 实测通过版本的二进制发布。发布资产为 Linux amd64/arm64 Master、Agent、安装脚本、升级脚本和校验文件；源码不在个人版二进制仓库中。

## 主要修正

1. 统一节点分享名称。复制节点和四类订阅输出都使用 `主机名-协议(+安全层)-Client`，避免协议、端口和客户端标识在不同入口显示不一致。
2. 统一订阅编排执行回执。新增、增加节点、减少节点和删除订阅都按节点顺序显示四阶段状态，并保留失败原因和完成入口。
3. 完善 Xray JSON 导入链路。导入识别、校验、配置写入和回显保留出站、路由及协议安全字段，不再静默丢弃根级配置。
4. 修复转发关联数据。客户端删除、转发编辑/删除和健康检测使用一致的绑定来源，减少“直连”误显示和残留规则。
5. 修正月度计费周期。客户端周期以到期日为锚点，周期流量用完只停用当前周期；下一个周期开始时自动清零并恢复，最终到期后不再恢复。
6. 补齐节点与客户端运维闭环。包含客户端明细、外显流量地址、备份恢复、配置导出、节点健康检查和 TG 通知/查询入口。
7. 改进安装与升级。Master 安装完成显示公网面板地址、管理员用户名和初始密码；`milk` 支持升级和彻底卸载；Agent 安装保留接管备份并兼容 systemd/OpenRC。
8. 收敛执行弹窗和管理页面。导入、编辑、删除、转发、快速生成和订阅任务统一使用四阶段回执样式，减少操作完成后无法关闭或状态不明的问题。

## 验证记录

- `go test ./...` 通过。
- `scripts/smoke-stage13.ps1` 通过：Base64、V2RayN、Shadowrocket、Sing-box、JSON、Clash 输出均有结果。
- `scripts/smoke-stage14.ps1` 通过：Root、`/ui/`、页面资源和节点 API 均返回 200。
- Linux amd64 Master 已上传至测试 VPS，`milk-master.service` 为 active，远端二进制 SHA256 与本地构建一致。

## 兼容说明

- 一键公开安装入口当前默认 Linux amd64；arm64 使用对应压缩包。
- Mihomo 是可选探测组件，缺失时不会阻止 Master 启动，但深度真实流量探测不可用。
- 请只在自有或已获授权的服务器上使用，并在公开发布前轮换测试环境凭据。
