# Milk Platform v3.0

## 发布范围

这是基于当前完整工作区重新构建的个人版二进制发布。发布资产包括 Linux amd64/arm64 Master、Agent、安装与管理脚本、完整安装包及 SHA256 校验文件。

## 主要变化

1. 新增 Agent 原生网络诊断链路。一次测试严格按 Traceroute、持续 TCPing、单线程测速顺序执行；TCPing 使用受控并发，测速保持单任务，避免争抢节点出口资源。
2. 新增 93 点持续 TCPing 目录。覆盖 31 个省级地区的电信、联通、移动目标；V3.0 删除独立教育网点位，并同步收口 Agent、Master、API、报告与测试契约。
3. 新增回程测试任务队列。Master 根据持久化 Command 聚合正在执行的主机和三阶段进度，刷新页面后仍可继续查看，不允许重复启动同一主机测试。
4. 新增外显网络质量报告。报告使用高熵公开令牌，展示线路、ASN、测速、重传、RTT 及省级延迟/丢包矩阵，并支持 Markdown 图片引用。
5. 优化 Agent 回执。探测结果随心跳提交，Master 记录任务 ID、开始/完成时间、失败原因和报告链接，网络测试进入统一审计链路。
6. 增强 IPv6 与双栈识别。节点卡片、客户端分享和订阅编排按实际地址能力处理 IPv4/IPv6，复制结果使用不改变页面布局的悬浮反馈。
7. 收敛运维页面。移除旧健康检测及 Mihomo 探针依赖，回程测试页保留主机入口、当前队列和测试记录，减少重复结果区域。
8. 延续 V2.0 的 Xray JSON 完整导入、转发绑定一致性、订阅编排、流量周期、TG 查询和 systemd/OpenRC 安装支持。

## 验证记录

- `go test ./...` 通过。
- 前端 TypeScript 类型检查通过。
- `scripts/smoke-stage14.ps1` 通过：Vite 生产构建和 Master Web 入口/资源/API 均返回 200。
- Agent/Master 连续目标目录契约测试通过：目标总数 93，旧 `cn-education-v4` 请求被拒绝。

## 升级提示

- V3.0 的持续 TCPing 目录版本为 `cn-province-carrier-v2`，Master 和 Agent 应一起升级。
- 旧报告中的教育网历史数据会在报告层过滤；旧任务记录仍保留在 Master 状态中。
- 一键安装入口继续从 GitHub `main` 获取最新稳定资产；固定版本可使用 `MILK_GITHUB_REF=v3.0.0`。
