# Milk Platform v3.01

## 发布范围

V3.01 是 V3.0 的安全与体验更新。发布资产由当前完整工作区重新构建，包含 Linux amd64/arm64 Master、Agent、安装与管理脚本、完整安装包及 SHA256 校验文件。

## 主要变化

1. 新增管理员登录白名单，默认关闭。支持 IPv4、IPv6 及对应 CIDR，保存前统一校验并规范化。
2. 白名单只约束网页登录会话；管理员 Token API 保留为故障恢复通道，避免错误配置导致 Master 无法管理。
3. 增加代理场景来源 IP 识别，配置界面展示当前识别地址；启用时必须包含当前管理员地址，防止即时自锁。
4. 白名单作为目标 Master 的本机安全策略独立保存，不随 `state.json` 导出，也不会被导入备份覆盖。
5. 管理员设置入口整合“登录白名单”弹窗，保留现有密码、Telegram、备份与还原交互。
6. 延续 V3.0 全部能力和数据兼容性；本版不包含 Cloudflare R2 自动灾备功能。

## 验证范围

- Domain 白名单匹配与规范化测试。
- Master Server 登录、代理来源地址、保存防自锁、Token 恢复与状态导入保护测试。
- React TypeScript 类型检查和 Vite 生产构建。
- Master Web Console smoke：嵌入资源、SPA 路由和 API 可用性。
- Linux amd64/arm64 Master 与 Agent ELF 构建、完整包内容和 SHA256 校验。

## 升级提示

- 白名单默认关闭，升级不会改变现有登录行为。
- 启用前先确认反向代理正确传递客户端地址，并在界面核对“当前来源 IP”。
- 若代理链配置错误导致网页登录受限，可使用管理员 Token API 修正或关闭白名单。
- 一键安装入口继续从 GitHub `main` 获取最新稳定资产；固定版本使用 `MILK_GITHUB_REF=v3.0.1`。

